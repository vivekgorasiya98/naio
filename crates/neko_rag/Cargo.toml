[package]
name = "neko_rag"
version.workspace = true
edition.workspace = true
description = "Vector RAG index + embeddings for Neko (nrag)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
fastembed = "5"
thiserror = "2"
